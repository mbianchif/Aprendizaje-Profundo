[project]
name = "report"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "numpy>=2.5.1",
    "orchestra",
    "safetensors>=0.8.0",
    "torch>=2.12.1",
]

[tool.uv.sources]
orchestra = { path = "../../tpp/orchestra-py", editable = true }
